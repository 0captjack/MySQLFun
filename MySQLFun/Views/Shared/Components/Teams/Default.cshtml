@{
}

@model IEnumerable<string>

@foreach (string t in Model)
    {
        <a asp-controller="Home" asp-action="Index" asp-route-bowlerTeam="@t"
           class="btn @(ViewBag.SelectedType == t ? "btn-primary" : "btn btn-outline-secondary")">@t</a>
           <br />
    }

